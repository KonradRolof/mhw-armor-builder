export default interface Defense {
  base: number; // The armor's base defense value
  max?: number; // The armor's defense value at max level
  augmented?: number; // The armor's defense value when augmented
}
