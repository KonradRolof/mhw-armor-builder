export default interface ArmorAssets {
  imageMale?: string; // URL to the male preview image, or null if one does not exist
  imageFemale?: string; // URL to the female preview image, or null if one does not exist
}
